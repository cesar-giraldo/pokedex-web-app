security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # Symfony 8: reemplaza hide_user_not_found. "none" oculta usuarios inexistentes
    # y evita filtrar detalles sensibles en respuestas de autenticación.
    expose_security_errors: none

    role_hierarchy:
        ROLE_DEVELOPER: ROLE_ADMIN
        ROLE_ADMIN: ROLE_USER

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: nickname

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Admin\Security\UserChecker
            login_throttling:
                max_attempts: 10
                interval: '15 minutes'
            form_login:
                login_path: app_admin_login
                check_path: app_admin_login
                enable_csrf: true
                username_parameter: _username
                password_parameter: _password
            logout:
                path: app_admin_logout
                target: app_admin_login
                invalidate_session: true
                delete_cookies:
                    REMEMBERME: { path: '/', samesite: lax }
            remember_me:
                name: REMEMBERME
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /
                samesite: lax

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/admin/login, roles: PUBLIC_ACCESS }
        - { path: ^/admin/logout, roles: PUBLIC_ACCESS }
        - { path: ^/admin, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
        firewalls:
            main:
                login_throttling:
                    max_attempts: 1000
                    interval: '1 minute'
