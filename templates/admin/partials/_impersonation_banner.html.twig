<div class="border-b border-warning-500/30 bg-warning-50 px-4 py-3 dark:border-warning-500/20 dark:bg-warning-500/10">
	<div class="mx-auto flex max-w-(--breakpoint-2xl) flex-wrap items-center justify-between gap-3">
		<p class="text-theme-sm font-medium text-warning-800 dark:text-warning-400">
			Estás viendo la aplicación como
			<strong>{{ app.user.name }} {{ app.user.lastname }} (@{{ app.user.nickname }})</strong>
		</p>

		<a href="{{ path('app_backend_home', {'_switch_user': '_exit'}) }}" data-turbo-prefetch="false"
			class="text-theme-sm font-semibold text-warning-800 underline-offset-2 transition hover:underline dark:text-warning-400">
			Volver a mi sesión
		</a>
	</div>
</div>
